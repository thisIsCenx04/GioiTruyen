spring:
  flyway:
    enabled: false

app:
  security:
    jwt-secret: ${JWT_SECRET:local-development-jwt-secret-at-least-32-bytes}

management:
  endpoint:
    health:
      group:
        readiness:
          include: "readinessState,db"
  health:
    redis:
      enabled: false
