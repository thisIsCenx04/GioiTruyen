openapi: 3.1.0
info:
  title: Intentionally invalid API fixture
  version: 1.0.0
paths:
  /seeded-violation:
    get:
      responses:
        "200":
          description: This operation intentionally violates the configured quality gate.
