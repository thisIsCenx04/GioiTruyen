apis:
  story-platform@v1:
    root: ./api/story-platform.v1.yaml

extends:
  - recommended-strict

rules:
  no-unused-components: error
  operation-4xx-response: error
  operation-singular-tag: error
  paths-kebab-case: error
  security-defined: error
